import { modelFields as Period } from '../validators/datatypes/period.validator';
import { modelFields as Address } from '../validators/datatypes/address.validator';

export const modelFields = {
  Period,
  Address,
};
